// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase : {
    apiKey: "AIzaSyAiGRpbshxKe6lL-L9V2EqUsbd6C0o3IDM",
    authDomain: "hello-world-fb-bd223.firebaseapp.com",
    databaseURL: "https://hello-world-fb-bd223.firebaseio.com",
    projectId: "hello-world-fb-bd223",
    storageBucket: "hello-world-fb-bd223.appspot.com",
    messagingSenderId: "246237500372",
    appId: "1:246237500372:web:aad0cb020513e02b"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
